/**
 * Node 엔트리(호환 래퍼)
 *
 * 실제 구현은 `dist/node/recommend-sdk-node.js`에 있습니다.
 * 기존 코드의 require 경로(`./recommend-sdk-node`)를 깨지 않기 위해 유지합니다.
 */

module.exports = require("./dist/node/recommend-sdk-node");
